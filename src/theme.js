import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        secondary: {
            light: '#ffffff',
            main: '#efebe9',
            dark: '#bdb9b7',
        },
        primary: {
            light: '#ffffb1',
            main: '#ffd180',
            dark: '#caa052',
        },
    },
});
export default theme;
